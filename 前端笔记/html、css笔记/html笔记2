相对路径:
		|-root
		|-a
		|--01.jpg
		|-b
		|--index.js
		./              当前路径(b)
        ../             上一级(root)
		
	问:	index.js访问01图片
	答:	../a/01.jpg     index.js文件在b文件夹下,去上一级路径(root),然后进入a文件夹,然后选择01.jpg文件
绝对路径:
	windows系统 绝对路径是指目录下的绝对位置，直接到达目标位置，通常是从盘符开始的路径。
	linux系统  LINUX系统中 绝对路径 以“/”为起始。
-----------------------------------------------------------------------------------------------------------
组标签,不能单独存在	
	<ul>标签:
		  无序列表,它的儿子只能是<li>标签,<li>标签必须被<ul>或<ol>包裹,不能单独存在,孙子可以是其他标签,	
		  导航栏通常都是ul无序列表。
		  搜索出的结果也可以放在<li>标签中展示。
	<ol>标签:
	    有序列表,同<ul>标签,只是语义不同,即使是用序号也不常用, 
	<dl>标签:
		定义列表,组标签,包含<dt>和<dd>标签, <dt> （定义列表中的项目）和 <dd> （描述列表中的项目）
-----------------------------------------------------------------------------
<div>标签,
	没有任样式效果,只有语义改变了,div中可以放置所有标签元素,所有元素在一个区域中 
    div标签是重要的布局标签,配合css可完成布局设计
	(div+css):
	     div标签负责布局,负责结构,负责分块。css负责样式
	标准的div+css页面,用的标签种类很少。
       div p h span a img ul ol dl input	
--------------------------------------------------------------------------------------
<span>标签:
	表达小区域的标签,是一个文本级标签。
	span里边只能放置文字/图片/表单元素。
	
---------------------------------------------------------------------------------------
按钮:
    <input type='button' />  普通按钮
	<input type='submit' />  提交
    <input type='reset' />  复位
-----------------------------------------------------------------------------------------------
字符实体:
    &lt;  转义成<
	&gt;  转义成>
	&nbsp; 转义成一个空格
	&copy; 转义成版权声明
---------------------------------------------------------------------------------------------
html废弃标签:
	html现在只描述语义,包含样式的 b u i del em strong hr 等标签被废弃,如果使用也只能作为css钩子使用,
	
---------------------------------------------------------------------------------------------
标签嵌套:
		1.块级元素 可嵌套任何元素;  注: p 不能嵌套div
		2.行内元素 只能嵌套行内元素
		3.a 除了不能嵌套a 和 input 其他无所不能
	
	



表单:
	表单就是收集用户信息的,
	<form>标签中包含表单内容 
		
		注!!!!!!:
			网页开发时根据语义选用标签
		
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	